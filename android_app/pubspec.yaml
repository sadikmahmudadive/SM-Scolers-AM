name: sm_scolers_app
description: SM Scolers Attendance — Flutter Android client (skeleton)
publish_to: none
version: 0.1.0
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.8.0
  firebase_database: ^10.1.0

# assets: []

flutter:
  uses-material-design: true
